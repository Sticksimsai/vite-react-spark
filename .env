# Base URL of the CULT Next.js backend that serves /api/beta/*.
# Leave empty to use same-origin (e.g. when proxied) — the UI falls back to example data if unreachable.
VITE_API_BASE=http://127.0.0.1:4317
VITE_PRIVY_APP_ID=
